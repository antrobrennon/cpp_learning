#include <iostream>
#include <cmath>
using namespace std;

int main(){
    float x, y;
    cin >> x >> y;
    // cout << max(x,y);
    cout << sqrt(x) << endl;
    cout << round(y) << endl;
    cout << log(2);
}