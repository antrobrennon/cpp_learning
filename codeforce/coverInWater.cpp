#include <bits/stdc++.h>
using namespace std;

int main() {
    int t, n;
    cin >> t >> n;
    string s;
    getline(cin,s);

    int count = 0;
    for(char c : s){
        if(c == '#'){
            count++;
        }
    }

    for(int i = 0 ; i < s.length() ; i++){
        
    }
}